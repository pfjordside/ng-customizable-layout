import { GetTemplateRefPipe } from './get-template-ref.pipe';

describe('GetTemplateRefPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTemplateRefPipe();
    expect(pipe).toBeTruthy();
  });
});
